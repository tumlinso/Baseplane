@PACKAGE_INIT@

include(CMakeFindDependencyMacro)
if(@BASEPLANE_HAS_CUDA@)
    find_dependency(CUDAToolkit)
endif()
if(@BASEPLANE_HAS_HIGHWAY@)
    find_dependency(hwy CONFIG)
endif()

include("${CMAKE_CURRENT_LIST_DIR}/BaseplaneTargets.cmake")
